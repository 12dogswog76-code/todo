# NTE-справочник: ревизия 12.09.2026

Список по обходу v34. Нумерация продолжает общий трекер.
Оценка: «S» — крупная работа, «M» — полдня, «S/M» — мелочь в пачке.

Один пункт (#164) исправлен сразу: он ломал целый раздел.

---

## 1. Карточка эспера (#151–#154)

**#151 · СДЕЛАНО 12.09 · Вернуть золотой цвет оружию ранга S.** Сейчас в списке
оружия имена S-ранга розово-фиолетовые. Причина найдена: в стилях стоит
`.row .r-S { color: var(--Incantation) }` — это цвет стихии «Заклинание»,
а не золото. Раньше было золото, вернуть.

**#152 · СДЕЛАНО 12.09 · Картинки оружия и картриджей в списках карточки.** По одним
названиям вещь не опознать. Картинки уже лежат: `img/arcs/` и
`img/suits/`, те же, что теперь стоят в быстрых фактах шапки.

**#153 · СДЕЛАНО 12.09 · Дизайн карточки — как в трекере ZZZ.** Там у карточки
фактурный фон («пупырышки»), за счёт него она не выглядит плоской
панелью. Перенести приём сюда: смотреть `zzz.html`, как сделан фон, и
повторить с цветом стихии.

**#154 · СДЕЛАНО 12.09 · Ссылка на prydwen и кнопка «закрыть».**

- надпись: не «гайд на prydwen ↗», а просто «prydwen»;
- сейчас она блёклая — сделать заметной;
- кнопку «✕ закрыть» тоже оформить приятнее, обе рядом и в одном духе.

---

## 2. Картриджи (#155–#158)

**#155 · СДЕЛАНО 12.09 · Раскрывать в ширину, а не в длину.** Сейчас раскрытый набор
уезжает вниз на два экрана. Содержимое разложить в несколько колонок,
как это сделано у оружия.

**#156 · СДЕЛАНО 12.09 · Убрать блок «Картриджи набора».** Ранги A и B повторяют
S и только удлиняют карточку. Раздел целиком убрать.

**#157 · СДЕЛАНО 12.09 · «Принимает модули» — подробно.** Сейчас четыре серых
плашки «тип II / тип III / тип IV». Нужно: по каждому типу — картинка
формы, что за характеристики даёт, сколько клеток занимает.

**#158 · СДЕЛАНО 12.09 · Общий список модулей — порядок.** Сначала все S-ранга, за
ними A, и внутри каждого ранга — порядок по типам (II, III, IV). Сейчас
типы перемешаны с рангами.

---

## 3. Команды (#159)

**#159 · СДЕЛАНО 12.09 · Вернуть сортировку по стихиям.** Была раскладка и по
эсперам, и по стихиям; сейчас осталась только по эсперам. Вернуть обе:
полоса портретов остаётся, плюс группировка составов по стихиям.

---

## 4. Коды (#160–#161)

**#160 · СДЕЛАНО 12.09 · Вернуть зелёную подсветку карточки кода.** При новой
раскладке (v33) она потерялась.

**#161 · ГОТОВО, ЖДЁТ ЗАЛИВКИ ВОРКЕРА · Награды показываются не полностью.** Видно «Фонс», а
сколько — нет. Причина: страница берёт коды с prydwen, а там у части
кодов состав записан словом без числа.

Разбор ntebuild уже написан (воркер версии 33, ntebuild первым
источником, prydwen запасным), но **воркер ещё не залит** — на сайте
сейчас работает 33-я версия страницы с 32-м воркером, поэтому коды
старые, от 18.05. После заливки проверить, что список пришёл с ntebuild:
внизу вкладки подпись «Коды с ntebuild.com».

Заливка — в браузере: дашборд Cloudflare → Workers → alextask-push →
Edit code → вставить `cloudflare\alextask-worker.js` целиком → Deploy.

---

## 5. Город (#162–#164)

**#162 · СДЕЛАНО 12.09 · Дома раскрывать в ширину**, как оружие, а не в длину.
Фотографии интерьера ушли вниз длинной колонкой.

**#163 · СДЕЛАНО 12.09 · Защита от падения отрисовки.** Когда `drawCity` падал
(см. #164), шапка с фильтрами уже была перерисована, а тело оставалось
от прошлой вкладки: на экране висел тир-лист с фильтрами транспорта.
Обернуть отрисовку вкладки и показывать понятное сообщение вместо
чужого содержимого.

**#164 · ИСПРАВЛЕНО 12.09 · Вкладка «Транспорт» не открывалась.**
В `nte-city.json` у 19 машин из 27 лежало `"skins": [null]` — в
PowerShell `@($null)` даёт массив из одного `$null`, а не пустой. На
первом же таком элементе отрисовка списка падала, и раздел не
показывался вовсе. Исправлено в двух местах: сборщик отсеивает пустые
(`Where-Object { $_ }`), страница страхуется `filter(Boolean)`.
Проверено прогоном: все 27 машин рисуются без ошибок.

---

## 6. Скины эспера (#165)

**#165 · СДЕЛАНО 12.09.** Ряд затемнённых миниатюр над артом отображается
неправильно — убрать его совсем. Вместо этого стрелки листания скинов
должны быть видны всегда, а не только при наведении на арт.

---

## 7. Карта (#166–#170)

**#166 · СДЕЛАНО 12.09 · Мылится при приближении.** Показываем плитку низкого
уровня и растягиваем. Нужно переключать уровень плиток по масштабу:
в наборе есть 6×6, 12×12, 25×25 и 51×51.

**#167 · ЧАСТИЧНО · Проверить значки и расположение.** Часть точек без своих
значков, часть стоит не на месте.

**#168 · СДЕЛАНО 12.09 · Колонку «Что показывать» — влево**, карту вправо.

**#169 · СДЕЛАНО 12.09 · Карту крупнее** на самой странице: сейчас она занимает
меньше половины ширины.

**#170 · S · Точек больше, чем 403.** В игровых таблицах лежат только
переходы, магазины и службы; сундуки, ресурсы и точки кражи хранятся
внутри уровней. Нужен другой источник — см. разведку чужих карт (#143).

**#171 · СДЕЛАНО 12.09 · Разбивка списка по типам.** Сейчас 80+ категорий подряд
одним столбцом. Сгруппировать: предметы, здания и службы, задания,
враги, точки интереса.

---

## 8. Хвосты

**#172 · Разобрать работу `nte-ocr.ps1`** — обещано вернуться к этому
позже. Проверить: ставится ли языковой пакет распознавания, как читает
игровой шрифт, хватает ли увеличения вдвое, не теряются ли нижние
строки, как и у серверного варианта.

Из прошлой ревизии не сделано и переносится: **#143** разведка чужих
интерактивных карт, **#145/#170** полные данные точек, **#148** проверка
привязки после полных данных.


---

## Что вошло в v35 (12.09, вечер)

Сделано всё, кроме #170 (полных данных точек нет в игровых таблицах) и
#172 (разбор OCR отложен по договорённости).

**Машины — сверх списка, по отдельной просьбе:**

- характеристики машины со стока и потолок с тюнингом. В `DT_VehicleData`
  поле `BasePropsScore` пустое у всех машин; заполнено оно у деталей, и
  игра складывает характеристики именно по ним. Сборщик повторяет этот
  расчёт: база — сумма по заводским деталям (`bWasDefault`), потолок —
  лучшая деталь в каждом узле по каждой характеристике отдельно;
- четыре характеристики перестали показываться ключами из таблицы:
  `UI_ShiftTime`, `UI_SuspensionHeight`, `UI_SuspensionDamp`, `UI_Drift`
  получили русские названия;
- карточка машины раскрывается в колонки, а не лентой вниз.

Проверено прогоном на данных: 20 машин из 27 имеют характеристики, у 11
тюнинг даёт прирост (Персьют V8: скорость 180 → 186, переключение 6 → 9).

**Карта.** Добавлен слой плиток 25×25 (625 штук, около 15 МБ) — при
сильном приближении страница переключается на него сама. Правило:
до 1.8× — 6×6, до 3.5× — 12×12, дальше 25×25. В json уходят только те
слои, что реально скачались: неполный слой дал бы дырки вместо карты.
Категории в колонке разложены по типам точек (переходы, магазины,
хранение, места), у каждого типа своя кнопка «все / снять».

**#167 частично:** значки сейчас есть у 359 точек из 403, у оставшихся
44 в таблице не указана картинка. Расположение проверяется только вместе
с полными данными (#170), поэтому пункт остаётся открытым.


---

## #113 — уточнение (13.09)

Сначала я понял вопрос неверно и разобрал «Ментальную карту» — испытание в
«Кроличьей норе». Речь же была о **пробуждениях**: дубликаты эспера с баннера
открывают ступени A1…A6, как созвездия в других играх.

Сделано в v40:

- сборщик читает `DataTable/Character/Awaken/<номер>AwakenEffect.json` —
  по файлу на эспера, и складывает их в `nte-awaken.json`. Имена файлов
  не единообразны: у Минт это `1019AwakenEffect1.json`, у части эсперов
  таблицы в дампе пока нет — такие пропускаются;
- игровая разметка из описаний срезается: `<NumGreen>40%</>` становится
  «40%», ссылки на термины — просто терминами;
- в карточке эспера появилась вкладка «Пробуждения»: ступени с названиями,
  описаниями и значками, отдельно два резонанса (их дают на третьем и
  шестом), переключатель «сколько у меня открыто» и отметка цели по оценке
  prydwen.

Раздел про «Ментальную карту» в механиках остаётся — он оказался полезен
сам по себе: там разобраны все режимы подземелий.


---

## #170 — точки карты, закрыто (13.09)

Алексей списался с Дэвидом, разработчиком nte.stardb.gg, и получил разрешение
использовать данные их карты. Они распаковали уровни игры и свели всё в
открытый набор: манифест `/map/index.json` → `/map/hethereau/manifest.*.json`,
маркеры разложены по «чанкам» вида `/map/hethereau/chunks/collectable-*.json`.

Сделано:

- новый сборщик `build-nte-map.ps1` качает манифест и чанки категории
  `collectable`, кладёт точки в `nte-map.json`, значки — в `img/mapicon/sd`.
  Категорию `debug` (служебные споунеры из дампа) не берём;
- **привязка карты теперь точная** — из их манифеста: `fx = (x - minX) / edge`,
  `fy = (y - minY) / edge`, ось Y не переворачивается. Проверено наложением на
  подложку: 93 % наших точек садятся на дороги и кварталы. Для сравнения:
  моя подобранная привязка давала 74 %, а исходное вписывание облака — 34 %;
- точки обоих источников лежат в одном списке, в колонке категорий чужие
  собраны в блок «Со StarDB» со ссылкой на источник, внизу вкладки —
  отдельная строка про разрешение.

Дальше остаётся #148 — сверить расположение на полном наборе.


---

## Разбор лога сборки (13.09)

Три источника шума в консоли, все вылечены:

**176 значков пробуждений отваливались с 404.** В таблице путь записан как
`UI/player/YH_UI_juexingICON/...`, а в репозитории ассетов папка называется
`Player` с заглавной. GitHub различает регистр — отсюда и 404 на каждой
картинке. Добавлено в `$DIRFIX`, туда же, где уже лежала такая же правка для
`Realestate`.

**Значка башни Вертхаймера не существовало.** Я когда-то вписал
`YH_UI_Mapicon_ta.png` вручную, и такого файла в репозитории нет. У половины
башен (тех, что приходят из `DT_MiniMapFixedIcons`) значок свой —
`YH_UI_map_icon3.png`; теперь он же ставится и телепортам, чтобы на карте
все башни выглядели одинаково.

**Повторы на 404.** Сборщик трижды долбился в файл, которого нет, и печатал
«связь оборвалась» — так было и с таблицами пробуждений тех эсперов, которых
ещё не выложили (Байцан, Чёрный дрозд, Линко). Теперь 404 не повторяется, а
в сообщении видно имя файла: раньше строки выглядели как
«не скачалось:  — 404» и понять, чего не хватает, было нельзя.


---

## v42 — страница не открывалась (13.09)

Симптом: на alextask.ru/nte остаётся одна шапка, тело пустое, в консоли
`Cannot access 'API_BASES' before initialization`.

Причина. Когда я добавлял события патча (v40), строка
`const EV_API = API_BASES.map(...)` встала в середине скрипта, а само
объявление `API_BASES` лежало почти в конце файла. `const` не всплывает,
в отличие от `function`: до строки объявления имя существует, но обращаться
к нему нельзя. Скрипт падал на этой строке ещё до отрисовки вкладок — то
есть страницу положил предыдущий деплой, а не сборка карты.

Фикс: `API_BASES` перенесён в шапку скрипта, к `APP_VER`. `OCR_API` и
`EV_API` теперь оба объявлены после него.

Заодно прогнал по всему скрипту проверку на такие же ловушки — обращений к
константам верхнего уровня раньше их объявления больше нет.


---

## Карта: размер, чёткость, скорость (v43)

**Мыло — нашлась настоящая причина.** Подложка раскладывалась CSS-сеткой:
скрипт задавал `grid-template-columns: repeat(n,1fr)`, а строки оставались
`auto`. У плитки в стилях `height:100%`, но проценты не от чего считать, пока
высота строки не известна, и браузер брал собственный размер картинки —
512 пикселей. Сетка 25×25 разрасталась до 12800 пикселей в высоту внутри
окна на тысячу с небольшим, и видно было то, что влезло сверху. Пороги
переключения слоёв тут вообще ни при чём — я их подкручивал зря.

Теперь плитки позиционируются поштучно, с явными `left/top/width/height` в
процентах. Заодно это дало отрисовку только видимых: на слое 25×25 в окне
лежит полтора-два десятка картинок вместо 625, и переключение уровня больше
не заметно глазом.

**Выбор слоя — по расчёту, а не по вбитым порогам.** Плитки все 512×512,
значит слой с сеткой n даёт полотно n×512: 3072, 6144 и 12800 пикселей.
Берём самый лёгкий, которому хватает разрешения на `сторона окна × зум ×
плотность экрана`. Старые пороги были подобраны под карту шириной в
полэкрана и на большом мониторе врали.

**Размер.** Окно перестало быть квадратом: теперь это прямоугольник во всю
ширину колонки, а квадратное полотно лежит внутри по центру. Высота — большее
из «высота экрана» и 62 % ширины окна, потолок 1600 пикселей: на 2K это около
полутора тысяч вместо тысячи, ценой небольшой прокрутки страницы. По бокам от
полотна теперь не пустые поля, а продолжение карты — при приближении есть куда
тащить. Плюс кнопка ⛶ — карта на весь монитор, выход по Esc.

**Скорость.** Точек стало 3765, и каждая — узел с тенью и картинкой; браузер
пересчитывал весь список на каждом кадре, карта дёргалась. Сделано:

- точки вне окна выключаются из отрисовки (`display:none`), доли держим в
  массиве — читать стили у каждой заново дороже, чем сравнить два числа;
- тяжёлое (плитки, отсев) откладывается до кадра через `requestAnimationFrame`:
  при перетаскивании события мыши приходят чаще, чем экран успевает обновиться.
  Сам сдвиг применяется сразу — его считает видеокарта;
- на окне карты `contain: layout paint`, чтобы панорамирование не заставляло
  браузер пересчитывать остальную страницу;
- `loading="lazy"` у плиток убран: слой лежит внутри `transform: scale`, и
  отложенная загрузка там работает непредсказуемо.

Колонка категорий теперь липнет к верху и прокручивается сама, а не уезжает
вместе со страницей.
